open! Syntax
open! Ir
module AlphaRename = struct end
module LiftExports = struct end

module ClosureConversion = struct
  (*
let closure_conversion (modul : Syntax.Module) =
  match modul with
  | Module (imports, toplevels, ) -> Ir.Types.Module () *)

end
