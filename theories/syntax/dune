(coq.theory
 (name RichWasm.syntax)
 (package iris-richwasm)
 (flags
   :standard
   -w -argument-scope-delimiter
   -w -automatic-prop-lowering)
 (theories Stdlib))

(rule
  (target rw.v)
  (deps prelude.v rw.sig)
  (action
    (run autosubst -o %{target} -f -no-static -p %{deps})))
